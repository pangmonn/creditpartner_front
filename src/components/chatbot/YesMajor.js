import React from 'react';
import BotBubble from './BotBubble';

const YesMajor = () => {
    const txt = '희망하는 학과에 관련된 정보는 학과 둘러보기에서 볼 수 있음.'

    return (
        <BotBubble text={txt} />
    );
};

export default YesMajor;